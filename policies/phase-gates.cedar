// phase-gates.cedar — methodology enforcement. Filled in Plan D (when triage/reporter exist).
@id("phase-gates-placeholder")
permit(
    principal == Agent::"audit-controller",
    action == Action::"orchestrate",
    resource == Resource::"engagement"
);

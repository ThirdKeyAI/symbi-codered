[tool]
name = "semgrep_ts"
version = "1.0.0"
binary = "codered"
description = "Semgrep with the TypeScript / JavaScript ruleset (via TypeScript scanner sidecar)"
timeout_seconds = 120
risk_tier = "low"

[tool.cedar]
resource = "Audit::StaticHunter"
action = "execute_tool"

[args.engagement_id]
position = 1
required = true
type = "string"

[command]
template = "codered semgrep_ts --engagement {engagement_id}"

[output]
format = "json"
envelope = true
